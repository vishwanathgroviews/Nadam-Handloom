node_modules
.expo
dist
web-build
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.env

# Native project generated by "expo prebuild" for local APK builds — the
# app stays managed, so this is build output, not source.
android/
ios/
credentials.json
*.jks
release-key/
